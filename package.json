{
  "name": "ulearn",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive courses that run entirely in the browser.",
  "type": "module",
  "scripts": {
    "build:registry": "node scripts/build-registry.mjs",
    "check:registry": "node scripts/build-registry.mjs --check",
    "serve": "python -m http.server 8000"
  },
  "license": "MIT"
}
